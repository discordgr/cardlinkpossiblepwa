apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CardLinkPossiblePwa.fullname" . }}-test-connection"
  labels:
{{ include "CardLinkPossiblePwa.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "CardLinkPossiblePwa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
